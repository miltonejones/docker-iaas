dockyard-ai.com {
  @internal path /internal /internal/*
  respond @internal 404
  reverse_proxy console:4300
}

# Custom-domain site blocks managed by the console — never edit this line.
import /data/sites/*.caddy
