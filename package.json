{
  "name": "dockyard",
  "version": "0.1.0",
  "private": true,
  "description": "Personal container management, EC2-style.",
  "workspaces": [
    "server",
    "web",
    "relay",
    "mcp",
    "cli"
  ],
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "dev:server": "npm --workspace server run dev",
    "dev:web": "npm --workspace web run dev",
    "dev:relay": "npm --workspace relay run dev",
    "dev:mcp": "npm --workspace mcp run dev",
    "dev:cli": "npm --workspace cli run dev",
    "build": "npm --workspace web run build && npm --workspace server run build && npm --workspace relay run build",
    "start": "npm --workspace server run start",
    "start:relay": "npm --workspace relay run start",
    "typecheck": "npm --workspace server run typecheck && npm --workspace web run typecheck && npm --workspace relay run typecheck && npm --workspace mcp run typecheck && npm --workspace cli run typecheck",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "npm --workspace server run test && npm --workspace web run test && npm --workspace relay run test && node --test scripts/*.test.mjs"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.8.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-react-hooks": "^7.1.1",
    "prettier": "^3.9.6",
    "typescript-eslint": "^8.65.0"
  }
}
